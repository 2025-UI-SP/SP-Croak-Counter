services:
  web:
    image: node:20-alpine
    working_dir: /app
    command: sh -c "npm ci && npm run dev -- --host 0.0.0.0"
    ports:
      - "5173:5173"
    environment:
      - NODE_ENV=development
    volumes:
      - /home/sen/Uni/SSP/SP-Croak-Counter:/app
      - /app/node_modules
    restart: unless-stopped


